1. might need to manully set the display environment variable as noted in the setup script
2. when running faster_rcnn_end2end.sh using command in README.md, the testing arg is not specified correctly, so have to test it manually after training

